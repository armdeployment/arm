---
title: Implementation Audit — Spec §9 vs Codebase
date: 2026-07-27
status: living
audience: engineering
---

# Implementation Audit

Generated by systematic comparison of `docs/arm-spec.md` §9 (Phase Plan), §14.1 (Invariants-as-code), §5.3 (Web UI IA), and §15 (Repo Layout) against the actual codebase on `main`.

Legend: ✅ done · 🔶 partial/stub · 🔴 missing · ➖ not yet in scope (future phase)

---

## 1.0 — Foundation

| Deliverable (spec ref) | Status | Evidence |
|---|---|---|
| Monorepo (pnpm + Turborepo) | ✅ | `package.json`, `turbo.json`, `pnpm-workspace.yaml` |
| Strict TS (`noUncheckedIndexedAccess`, `verbatimModuleSyntax`) | ✅ | `tsconfig.base.json` |
| ESLint config | 🔴 | `eslint` in devDeps but **no config file** — lint scripts fail |
| Prettier | ✅ | `.prettierrc.json` |
| Repo governance: `AGENTS.md` | ✅ | updated with run commands |
| Repo governance: `docs/CONCEPTS.md` | ✅ | 22+ domain terms |
| Repo governance: Makefile (pinned versions) | ✅ | `Makefile` |
| CI skeleton (typecheck + guardrails + contract) | ✅ | 4 workflows in `.github/workflows/` |
| Tiered pre-push gate (.husky) | 🔴 | not configured |
| CI sync check (AGENTS.md table ↔ workflows) | 🔴 | no automated check |
| Executable guardrails for §11 invariants | 🔶 | 4/8 implemented (see guardrail table below) |
| Postgres schema (22 tables, full §4.1) | ✅ | `packages/db/drizzle/0000_far_drax.sql` |
| ClickHouse schemas (2 event tables) | ✅ | `packages/clickhouse/migrations/0001_init.sql` |
| Web shell: Next.js + route guards (UI RBAC) | 🔶 | Next.js app exists; **no route guards/middleware** |
| Web shell: design system pinned (§5.3) | ✅ | Tailwind v4 + Recharts + TanStack Table |
| Web shell: lo-fi wireframes | 🔴 | `docs/wireframes.md` not created |
| Auth: OIDC SSO + RBAC + ARM-as-OIDC-issuer | 🔴 | `packages/auth` is 2-line stub |
| tRPC routers for CRUD/query | 🔴 | `packages/trpc` is 2-line stub |
| Own-telemetry baseline (OTel) | 🔴 | no OTel deps or instrumentation anywhere |
| `packages/proto` event zod schemas | 🔴 | 2-line stub — no zod contracts |
| `packages/config` env validation | 🔴 | 2-line stub |
| `packages/policy` resolver skeleton | 🔴 | 2-line stub |

**1.0 completion: ~55%.** Schema + guardrails + CI + dashboard shell are solid. The package logic layer (proto, config, auth, trpc, policy) and telemetry are the critical gaps.

---

## §14.1 Invariants-as-code — guardrail status

| Invariant | Guardrail | Status |
|---|---|---|
| §11.1 prompt bodies never leave VPC | `no-content-egress` | 🔶 check exists; missing proto zod schemas, egress allowlist lint, proxy bundle test |
| §11.3 deny-wins | property-based tests on resolver | 🔴 no resolver, no property tests |
| §11.6 + D1 tenant isolation | `tenant-isolation` | 🔶 check exists; missing tRPC tenant-scope middleware test |
| §11.7 accountable stakeholder | DB constraint + API validation | 🔶 DB constraint ✅; API validation test 🔴 |
| Event-shape stability | zod contract tests on `packages/proto` | 🔴 proto is stub |
| LLM trust boundary | `safe-render` | 🔴 not implemented |
| Master-key custody | `no-secret-dumps` | ✅ implemented + mutation-proofed |
| Policy-cache freshness (D5) | data-plane reports version | ➖ data plane not built yet |
| Dependency security | `pnpm audit` baselined | 🔶 workflow exists; baselining mechanism missing |

**Guardrails: 4/9 fully implemented.** Priority fills: `safe-render`, proto zod contracts.

---

## §5.3 Web UI — route status

| Route | Status | Notes |
|---|---|---|
| `/` Dashboard | ✅ | stat cards, spend chart, model chart, tier pie, agents table |
| `/agents` | 🔴 | not created |
| `/spend` | 🔴 | not created |
| `/access` | 🔴 | not created |
| `/audit` | 🔴 | not created |
| `/settings` | 🔴 | not created |
| `/onboarding` | 🔴 | not created |

**Design system components built:** `stat-card`, `charts` (3 chart types), `agents-table`, `sidebar`.
**Missing per §5.3:** org-tree-explorer, savings-estimator (impact preview), notification-center, high-stakes-action-pattern (confirm modal), deferred-shell-panel.

---

## 1.1–1.4 — future phases (all ➖ not started)

| Phase | Key deliverables | Status |
|---|---|---|
| 1.1 Metering | billing connectors, delegate-key minting, workers, advanced dashboards | ➖ all stubs |
| 1.2 Data Plane | Hono proxy, vLLM gateway, meter-agent, Docker/Helm/Terraform, CLIs | ➖ all stubs, `infra/` dir missing |
| 1.3 Cloud Connectors | permission engine, S3/GCS, policy simulator, live IdP test | ➖ all stubs |
| 1.4 Data+Collab | DB/SharePoint connectors, JIT approval, classification enforcement | ➖ all stubs |

---

## §14.3 Repo hygiene gaps

| Item | Status |
|---|---|
| `docs/solutions/` with D1/D2/D5 records | 🔴 not created |
| CI sync check (AGENTS.md ↔ `.github/workflows/`) | 🔴 |
| Pre-push gate (tiered: state + tree dependent) | 🔴 |

---

## Priority action list (fills 1.0 gaps, highest impact first)

1. **`packages/proto` zod event schemas** — foundational contract; unblocks trpc, billing, guardrails contract tests
2. **`packages/config` env + secrets validation** — foundational; every service needs it
3. **`guardrail: safe-render`** — spec §14.1 explicitly mandates it; XSS guard for LLM strings
4. **ESLint config** — lint scripts currently broken
5. **`docs/solutions/` D1/D2/D5 records** — spec §14.3 repo hygiene
6. **`packages/auth` OIDC + RBAC skeleton** — needed before any route guards
7. **`packages/trpc` router skeleton + tenant middleware** — needed before live dashboards
8. **`packages/policy` resolver skeleton + deny-wins property tests** — §11.3 guardrail
9. **OTel telemetry baseline** — spec §9 1.0 deliverable
10. **Additional UI routes** (/agents, /spend, /access, /audit) — §5.3 IA
