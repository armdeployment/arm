.DS_Store
# scratch/local files
test-glm5.py
__pycache__/
*.pyc
node_modules/
.env
.env.*
!.env.example

# build/CI artifacts
.turbo/
dist/
.next/
coverage/
*.tsbuildinfo
packaging/.build/

# next.js
next-env.d.ts
apps/control-plane/web/test-results/
apps/control-plane/web/playwright-report/
apps/control-plane/web/.next/
apps/public/test-results/
apps/public/playwright-report/
apps/public/.next/
apps/public/out/
.gstack/
demo/arm-demo.mp4
skills-lock.json

# arm-video Remotion build output + renders
apps/arm-video/build/
apps/arm-video/out/

# local scratch — never commit
_to_delete/
releases/

# Terraform / OpenTofu working files. `infra/terraform` is a reusable module,
# not a root module, so the lock file belongs to whoever consumes it — and
# .terraform/ is 700MB of downloaded providers.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
