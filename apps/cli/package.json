{
  "name": "@arm-app/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "arm": "tsx src/index.ts",
    "setup": "tsx src/index.ts setup",
    "doctor": "tsx src/index.ts doctor"
  },
  "dependencies": {
    "@arm/proto": "workspace:*",
    "@arm/config": "workspace:*",
    "@arm/trpc": "workspace:*",
    "@arm/client-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "tsx": "4.20.6",
    "vitest": "4.1.10"
  }
}
